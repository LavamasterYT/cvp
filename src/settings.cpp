#include "settings.h"

namespace settings {
    std::string file = "";
    bool playAudio = false;
    bool showUI = true;
    bool debug = false;
    Console::ColorMode colorMode = Console::MODE_ASCII;
}
